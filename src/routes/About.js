import React from 'react';

function About(prop) {
    console.log(prop)
    return (
        <section className="container"> 
            <div>About</div>
        </section>
    );
}

export default About;